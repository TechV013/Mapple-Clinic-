services:
  - type: web
    name: maple-clinic
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    healthCheckPath: /
    nodeVersion: 22.0.0
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_TELEMETRY_DISABLED
        value: "1"
